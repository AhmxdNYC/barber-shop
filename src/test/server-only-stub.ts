/**
 * Stub for the `server-only` package under Vitest.
 *
 * The real module throws when imported outside a React Server Component,
 * which is exactly what we want in the app and exactly what breaks a Node
 * test runner. Aliased in vitest.config.mts.
 */
export {};
